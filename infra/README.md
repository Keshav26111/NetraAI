# infra

Deployment configuration. **Not yet implemented.**

Planned: `docker-compose.yml` (db, api, web) and a `Caddyfile` (TLS reverse proxy). Kubernetes and
managed-cloud setups are out of scope for the MVP.

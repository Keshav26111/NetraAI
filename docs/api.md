# API

**Status: TBD.** No API is implemented yet (Phase 2). The high-level endpoint groups are defined in
[architecture.md](architecture.md), section 7. The FastAPI OpenAPI schema will become the source of
truth once the backend exists.

To fill in later: authentication scheme, error format, pagination, the `AnalysisResult` schema
(see the inference contract in [architecture.md](architecture.md), section 5), and versioning
policy (`/api/v1`).
